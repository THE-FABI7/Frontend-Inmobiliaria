/**
 * Represents an item in a menu. 
 * @property {string} [idMenu] - The ID of the menu item.
 * @property {string} [ruta] - The URL of the page that the menu item links to.
 * @property {string} [texto] - The text to display for the menu item.
 * @property {string} [icono] - The icon to display for the menu item.
 */
export class itemMenuModel {
  idMenu?: string;
  ruta?: string;
  texto?: string;
  icono?: string;
}
